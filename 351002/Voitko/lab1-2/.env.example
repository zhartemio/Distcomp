# Корень проекта (как в Egorov/lab2). Скопируйте в .env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=distcomp
DB_SCHEMA=distcomp
# БД для первого подключения при авто-создании DB_NAME (если БД ещё нет)
# DB_MAINTENANCE_DB=postgres

# Опционально: полная строка (pytest подставляет сама)
# DATABASE_URL=postgresql+psycopg2://user:pass@localhost:5432/distcomp
