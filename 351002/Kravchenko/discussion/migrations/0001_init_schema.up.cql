CREATE TABLE IF NOT EXISTS tbl_note (
                                        issue_id bigint,
                                        id bigint,
                                        content text,
                                        PRIMARY KEY (id)
);